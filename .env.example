# KickPay AI — Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.

# Optional: Postgres connection for server-side relay data
DATABASE_URL=postgresql://user:password@host:5432/kickpay

# Optional: JWT signing secret for the relay server
JWT_SECRET=replace-with-a-long-random-string
